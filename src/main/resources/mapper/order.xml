<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="itbank.pethub.model.OrderDAO">
    <!-- Cart 관련 쿼리 -->
    <select id="findCartByMemberId" resultType="com.example.demo.vo.CartVO">
        SELECT * FROM cart WHERE member_id = #{memberId}
    </select>

    <select id="findCartItemsByCartId" resultType="com.example.demo.vo.CartItemVO">
        SELECT * FROM cart_item WHERE cart_id = #{cartId}
    </select>

    <insert id="addItemToCart">
        INSERT INTO cart_item (cart_id, item_id, item_name, quantity, price)
        VALUES (#{cartId}, #{itemId}, #{itemName}, #{quantity}, #{price})
    </insert>

    <delete id="removeItemFromCart">
        DELETE FROM cart_item WHERE id = #{cartItemId}
    </delete>

    <update id="updateItemQuantity">
        UPDATE cart_item SET quantity = #{quantity} WHERE id = #{cartItemId}
    </update>

    <!-- Order 관련 쿼리 -->
    <insert id="createOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (member_id, status, total_price, created_at)
        VALUES (#{memberId}, #{status}, #{totalPrice}, now())
    </insert>

    <insert id="createOrderItem">
        INSERT INTO order_item (order_id, item_id, item_name, quantity, price)
        VALUES (#{orderId}, #{itemId}, #{itemName}, #{quantity}, #{price})
    </insert>

    <update id="updateOrderStatus">
        UPDATE orders SET status = #{status} WHERE id = #{orderId}
    </update>

    <!-- Member 관련 쿼리 -->
    <select id="findMemberById" resultType="com.example.demo.vo.MemberVO">
        SELECT * FROM member WHERE id = #{memberId}
    </select>

</mapper>